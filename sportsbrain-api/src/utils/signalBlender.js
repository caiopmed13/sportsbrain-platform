// ─────────────────────────────────────────────────────────────
// PROPRIETARY MODULE — implementation withheld from the public portfolio.
// Blends multiple independent quality signals (form, lineup, market
// movement, head-to-head, rest, momentum, officials) into a single
// probability adjustment per pick, and generates human-readable rationale.
// ─────────────────────────────────────────────────────────────

const __WITHHELD = 'Proprietary module — implementation withheld from public portfolio.';

export function blendSignals(baseProb, signals) { throw new Error(__WITHHELD); }
export async function getLineupSignal(pick, env) { throw new Error(__WITHHELD); }
export async function getRefereeSignal(pick, env) { throw new Error(__WITHHELD); }
export function generatePickExplanation(pick, signals) { throw new Error(__WITHHELD); }
export async function getSignalsForPick(pick, env) { throw new Error(__WITHHELD); }
